{
  "license": "BSD-3-Clause",
  "scripts": {
    "lint": "prettier -c {src,test}/**/*.js",
    "postinstall": "bower install",
    "prettify": "prettier --write {src,test}/**/*.js",
    "test": "pulp build -I test -- +RTS -N2 -RTS && jest"
  },
  "devDependencies": {
    "css-tree": "^1.0.0-alpha.34",
    "jest": "^23.6.0",
    "prettier": "^1.18.2"
  }
}
